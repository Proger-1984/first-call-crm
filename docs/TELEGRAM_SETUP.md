# Настройка Telegram авторизации

## 📱 Шаг 1: Настройка домена в BotFather

Telegram требует, чтобы домен был зарегистрирован для виджета авторизации.

### Вариант A: Использовать существующий домен

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/setdomain`
3. Выберите вашего бота `@firstcall_service_bot`
4. Укажите домен: **`local.firstcall.com`**
5. Подтвердите изменения

### Вариант B: Использовать ngrok для локального тестирования

Если домен `local.firstcall.com` не работает (так как это локальный домен), используйте ngrok:

```bash
# Установите ngrok (если не установлен)
# https://ngrok.com/download

# Запустите туннель на порт 3002 (React dev server)
ngrok http 3002
```

Ngrok даст вам публичный URL, например: `https://abc123.ngrok.io`

Затем:
1. Откройте [@BotFather](https://t.me/BotFather)
2. `/setdomain`
3. Выберите бота
4. Укажите: `abc123.ngrok.io` (без https://)

## 🧪 Тестирование

### Для локального тестирования БЕЗ реального Telegram:

1. Откройте: http://localhost:3002/login-test
2. Нажмите "Войти (Тестовый режим)"

**Примечание:** Тестовый режим НЕ РАБОТАЕТ, так как бэкенд проверяет подпись Telegram.

### Для реального тестирования С Telegram:

1. Настройте домен в BotFather (см. выше)
2. Откройте: http://localhost:3002/login (или ваш ngrok URL)
3. Нажмите кнопку Telegram
4. Авторизуйтесь через Telegram

## 🔧 Альтернатива: Тестовый режим в бэкенде

Если нужен тестовый режим для разработки, можно добавить в `.env`:

```env
APP_ENV=development
SKIP_TELEGRAM_VALIDATION=true  # Только для локальной разработки!
```

И модифицировать `TelegramAuthController.php` для пропуска валидации в dev режиме.

## ✅ Проверка настройки

После настройки домена обновите страницу и проверьте:
- Виджет Telegram должен загрузиться без ошибки "Bot domain invalid"
- При клике появится окно Telegram для авторизации

---

## ⚠️ Известная проблема: "Bot domain invalid"

**Что это значит:**
- Telegram виджет успешно загружается
- API бэкенда отвечает корректно
- НО: Telegram требует, чтобы домен был зарегистрирован в BotFather

**Почему происходит:**
- Telegram Widget требует, чтобы домен приложения был добавлен в настройки бота
- Локальный домен `local.firstcall.com` не может быть зарегистрирован в Telegram

**Решение:** Используйте ngrok (см. Вариант B выше)
