---
description: 
alwaysApply: true
---

# 🚀 First Call CRM — Контекст проекта

> Это правило загружается автоматически при каждом новом чате.
> Содержит базовый контекст проекта, чтобы AI сразу понимал архитектуру.

---

## 📋 О проекте

**First Call CRM** — REST API сервис для управления подписками на недвижимость с автоматическими уведомлениями через Telegram Bot.

**Основной функционал:**
- Авторизация через Telegram Login Widget
- Управление подписками на объявления недвижимости
- Парсинг объявлений с CIAN, Avito
- Автозвонок по новым объявлениям
- Telegram уведомления

---

## 🛠️ Технологический стек

### Backend
| Технология | Версия | Назначение |
|---|-----|---|
| PHP | 8.3+ | Строгая типизация, современный синтаксис |
| Slim 4 | 4.x | Легковесный REST API framework |
| PHP-DI | 7.x | Dependency Injection контейнер |
| Eloquent ORM | 10.x | Работа с БД через модели |
| PostgreSQL | 15 | Основная БД |
| PostGIS | 3.x | Геопространственные запросы |
| JWT | - | Авторизация (access + refresh токены) |

### Frontend
| Технология | Версия | Назначение |
|---|-----|---|
| React | 18.3+ | UI библиотека |
| TypeScript | 5.9+ | Типизация |
| Vite | 5.4+ | Сборка |
| Zustand | 5.0+ | State management |
| TanStack Query | 5.90+ | Серверный state |
| Axios | 1.9+ | HTTP клиент |
| React Router | 7.12+ | Роутинг |

### Инфраструктура
| Сервис | Контейнер | Порт |
|-----|-----|---|
| PostgreSQL + PostGIS | slim_postgres | 5432 |
| PHP-FPM 8.3 | slim_php-fpm | 9000 |
| PHP CLI (команды) | slim_php-cli | — |
| Nginx (SSL) | slim_nginx | 80, 443 |
| pgAdmin 4 | slim_pgadmin | 5050 |

---

## 🏗️ Архитектура (слои)

```
HTTP Request (Nginx)
        ↓
┌─────────────────────────────────────┐
│        Middleware Layer             │
│  CORS → JSON Parser → Auth (JWT)    │
└─────────────────┬───────────────────┘
                  ↓
┌─────────────────────────────────────┐
│        Controller Layer             │
│  Валидация → Делегирование          │
│  Использует ResponseTrait           │
└─────────────────┬───────────────────┘
                  ↓
┌─────────────────────────────────────┐
│         Service Layer               │
│  Бизнес-логика, интеграции          │
│  Инжектируются через DI             │
└─────────────────┬───────────────────┘
                  ↓
┌─────────────────────────────────────┐
│          Model Layer                │
│  Eloquent модели, связи             │
│  Аксессоры/мутаторы                 │
└─────────────────┬───────────────────┘
                  ↓
      PostgreSQL Database
```

---

## 📁 Структура проекта

### Backend (`/home/sokol/first-call/`)
```
├── bootstrap/           # Инициализация
│   ├── app.php         # Конфигурация + Eloquent
│   ├── container.php   # DI контейнер (регистрация сервисов)
│   └── middleware.php  # Глобальные middleware
├── routes/routes.php   # ВСЕ API маршруты
├── src/
│   ├── Controllers/    # Тонкие контроллеры
│   ├── Services/       # Бизнес-логика
│   ├── Models/         # Eloquent модели
│   ├── Middleware/     # PSR-15 middleware
│   └── Commands/       # CLI команды
├── db/migrations/      # Миграции БД
└── docs/api/openapi.yaml # OpenAPI спецификация
```

### Frontend (`frontend-react/`)
```
├── src/
│   ├── components/
│   │   ├── Layout/     # Header, Sidebar
│   │   ├── UI/         # Переиспользуемые компоненты
│   │   └── Auth/       # Авторизация
│   ├── pages/          # Страницы (Dashboard, Login)
│   ├── stores/         # Zustand stores
│   ├── services/api.ts # Axios клиент
│   ├── types/          # TypeScript типы
│   └── styles/         # CSS (переменные, глобальные стили)
└── public/             # Статика (logo, favicon)
```

---

## 🗄️ Основные таблицы БД

| Таблица | Назначение |
|---|---|
| `users` | Пользователи (Telegram авторизация) |
| `user_subscriptions` | Подписки пользователей |
| `tariffs` | Тарифные планы |
| `tariff_prices` | Цены по локациям |
| `categories` | Категории (Аренда/Продажа) |
| `locations` | Города/регионы |
| `listings` | Объявления недвижимости |
| `listing_statuses` | Статусы объявлений |
| `refresh_tokens` | JWT refresh токены |
| `sources` | Источники (CIAN, Avito) |

**Полная схема:** `docs/DATABASE.md` (22 таблицы)

---

## 🔐 JWT Авторизация

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   Client    │────>│  /auth/     │────>│   Server    │
│             │     │  telegram   │     │             │
└─────────────┘     └─────────────┘     └─────────────┘
                           │
                           ↓
              ┌─────────────────────────┐
              │  Возвращает:            │
              │  - access_token (15min) │
              │  - refresh_token (30d)  │
              └─────────────────────────┘
```

**Заголовок:** `Authorization: Bearer <access_token>`

**Обновление:** `POST /api/v1/auth/refresh` с `refresh_token` в body

---

## 🌐 API Endpoints (основные)

| Метод | URL | Описание |
|----|-----|----|
| POST | `/api/v1/auth/telegram` | Авторизация через Telegram |
| POST | `/api/v1/auth/refresh` | Обновление токенов |
| GET | `/api/v1/user/profile` | Профиль пользователя |
| GET | `/api/v1/subscriptions` | Список подписок |
| POST | `/api/v1/subscriptions` | Создание подписки |
| GET | `/api/v1/tariffs` | Список тарифов |
| GET | `/api/v1/listings` | Список объявлений |

**Полная документация:** `docs/api/openapi.yaml`

---

## 🚀 Быстрые команды

```bash
# Запуск всего
docker-compose up -d

# Frontend dev
cd frontend-react && npm run dev

# Миграции
make migrate

# Логи
docker-compose logs -f

# Войти в PHP контейнер
docker exec -it slim_php-cli bash
```

---

## 📚 Что читать при работе с проектом

### ОБЯЗАТЕЛЬНО прочитай перед началом работы:

1. **`.cursor/PROJECT_STATUS.md`** — текущая задача, контекст, блокеры
2. **Правила кодирования:**
   - Backend: `.cursor/rules/firstcall-backend.mdc` (1800+ строк)
   - Frontend: `.cursor/rules/firstcall-frontend-react.mdc` (760+ строк)

### По необходимости:

| Файл | Когда читать |
|---|-----|
| `docs/ARCHITECTURE.md` | Понять архитектуру |
| `docs/DATABASE.md` | Работа с БД |
| `docs/API-QUICK-REFERENCE.md` | Работа с API |
| `docs/DEPLOYMENT.md` | Деплой/запуск |
| `docs/GLOSSARY.md` | Непонятные термины |

---

## ⚠️ Важные правила

1. **Не изменяй старые миграции** — создавай новые
2. **Всегда используй типизацию** — `declare(strict_types=1)` в PHP, TypeScript во фронте
3. **Контроллеры тонкие** — вся логика в сервисах
4. **ResponseTrait** — для унифицированных ответов API
5. **Обновляй PROJECT_STATUS.md** — после завершения задач

---

## ✅ Чеклист перед завершением задачи

При изменении **API** ОБЯЗАТЕЛЬНО:
- [ ] Обновить `docs/api/openapi.yaml` — полное описание эндпоинта со всеми кодами ответов (200, 400, 401, 404, 409, 500)
- [ ] Обновить `docs/API-QUICK-REFERENCE.md` — краткое описание эндпоинта
- [ ] Добавить `try-catch` в новые методы контроллеров
- [ ] Проверить что все HTTP коды соответствуют документации

При изменении **БД** ОБЯЗАТЕЛЬНО:
- [ ] Создать новую миграцию (НЕ изменять старые!)
- [ ] Обновить `docs/DATABASE.md` — описание таблицы, связи, индексы
- [ ] Добавить миграцию в список выполненных в `PROJECT_STATUS.md`

При изменении **Frontend** ОБЯЗАТЕЛЬНО:
- [ ] Добавить TypeScript типы для новых данных
- [ ] Обработать loading/error состояния
- [ ] Проверить что стили соответствуют существующим компонентам

---

## 📋 Ведение статуса проекта

### Когда обновлять `.cursor/PROJECT_STATUS.md`:

| Событие | Действие |
|---|----|
| Завершена задача | Перенести в "✅ Что сделано" |
| Начата новая задача | Обновить "⚠️ ТЕКУЩАЯ ЗАДАЧА" |
| Обнаружен баг | Добавить в "Известные проблемы" |
| Добавлена миграция | Добавить в "Выполненные миграции" |
| Принято решение | Добавить в "Принятые решения" |

### Секция "Контекст для AI" в начале файла:

```markdown
## 📝 Контекст для AI

### Последняя сессия
**Дата:** [дата]
**Что делали:** [краткое описание]

### Текущий фокус
- [над чем работаем]

### Важно помнить
- [ключевые детали]

### Блокеры
- [что мешает] или "Нет"
```

---

## 🔗 Полезные ссылки

| Ресурс | URL |
|-----|-----|
| Frontend | https://local.firstcall.com |
| Backend API | https://local.firstcall.com/api/v1 |
| pgAdmin | http://localhost:5050 |
| API docs (Redoc) | http://localhost:8080/redoc.html |
