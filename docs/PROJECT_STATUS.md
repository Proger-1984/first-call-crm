# First Call CRM - Статус проекта

**Дата обновления:** 25 января 2026

---

## 🚀 Быстрый старт

### Шаг 1: Запуск Backend + БД (Docker)

```bash
cd /home/sokol/first-call

# Запуск всех контейнеров
docker-compose up -d

# Или через Makefile
make up

# Проверить статус
docker-compose ps
```

**Что запускается:**
| Контейнер | Сервис | Порт |
|-----------|--------|------|
| slim_postgres | PostgreSQL 15 + PostGIS | 5432 |
| slim_php-fpm | PHP-FPM 8.3 | 9000 (internal) |
| slim_php-cli | PHP CLI (команды) | — |
| slim_nginx | Nginx (reverse proxy + SSL) | 80, 443 |
| slim_pgadmin | pgAdmin 4 | 5050 |

### Шаг 2: Запуск Frontend (React)

```bash
cd /home/sokol/first-call/frontend-react

# Установить зависимости (если не установлены)
npm install

# Запуск dev-сервера
npm run dev
# По умолчанию: http://localhost:3000

# Или с указанием порта
npm run dev -- --port 3002 --host
```

### Шаг 3: Сборка для production

```bash
cd /home/sokol/first-call/frontend-react

# Сборка
npm run build

# Результат в папке dist/, раздаётся через Nginx
```

---

## 🌐 URLs

| Сервис | URL | Описание |
|--------|-----|----------|
| **Приложение (prod)** | https://local.firstcall.com | Через Nginx + SSL |
| **Frontend dev** | http://localhost:3000 | Vite dev server |
| **API** | https://local.firstcall.com/api/v1 | REST API |
| **pgAdmin** | http://localhost:5050 | Управление БД |
| **API документация** | http://localhost:8090/redoc.html | OpenAPI/Redoc |

---

## 🗄️ База данных

### Подключение к pgAdmin (http://localhost:5050)

**Логин в pgAdmin:**
- Email: `admin@example.com`
- Password: `admin`

**Добавление сервера PostgreSQL:**
- Host: `postgres` (имя контейнера в Docker network)
- Port: `5432`
- Database: `slim_api`
- Username: `postgres`
- Password: `postgres`

### Подключение напрямую (из хоста)

```bash
# Через psql
psql -h localhost -p 5432 -U postgres -d slim_api

# Или через Docker
docker exec -it slim_postgres psql -U postgres -d slim_api
```

### Миграции

```bash
# Запуск миграций
make migrate

# Или вручную
docker exec -it slim_php-cli php db/migrations/run.php
```

### ⚠️ Важно о данных

```bash
# Остановка БЕЗ удаления данных
docker-compose down

# Остановка С УДАЛЕНИЕМ данных (volumes)
docker-compose down -v  # ОСТОРОЖНО! Удалит все данные БД!
```

### 🔄 Полная пересборка с нуля

```bash
# Остановить всё и удалить данные
docker-compose down -v

# Пересобрать образы
docker-compose build --no-cache

# Запустить
docker-compose up -d

# Подождать запуска PostgreSQL
sleep 5

# Установить зависимости и миграции
make install && make migrate

# Пересобрать фронтенд
cd frontend-react && npm install && npm run build
```

**Одной командой:**
```bash
docker-compose down -v && docker-compose build --no-cache && docker-compose up -d && sleep 5 && make install && make migrate
```

Подробнее: [DEPLOYMENT.md](./DEPLOYMENT.md#полная-пересборка-с-нуля-reset)

---

## 🛠️ Полезные команды

### Docker

```bash
# Статус контейнеров
docker-compose ps

# Логи всех сервисов
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f nginx
docker-compose logs -f php-fpm

# Перезапуск
docker-compose restart

# Пересборка образов
docker-compose build --no-cache
docker-compose up -d

# Войти в контейнер PHP
docker exec -it slim_php-cli bash
```

### Frontend

```bash
cd frontend-react

# Dev сервер
npm run dev

# Сборка
npm run build

# Предпросмотр сборки
npm run preview

# Проверка типов TypeScript
npx tsc --noEmit

# Линтинг
npm run lint
```

### Makefile команды

```bash
make up        # Запуск Docker
make down      # Остановка Docker
make build     # Пересборка образов
make logs      # Просмотр логов
make migrate   # Запуск миграций
make install   # Установка composer зависимостей
make init      # Полная инициализация проекта
```

---

## ✅ Что сделано

### Авторизация
- [x] Telegram Login Widget интеграция
- [x] JWT токены (access + refresh)
- [x] Автоматическое обновление токенов
- [x] Logout функционал
- [x] Protected routes

### Header (шапка)
- [x] Логотип "FIRST CALL" (SVG)
- [x] Кнопка "Поддержка" → открывает https://t.me/firstcall_support
- [x] Кнопка "Биллинг"
- [x] Информация о подписке (дата, остаток дней)
- [x] Статус автозвонка (переключение через API)
- [x] Аватар пользователя с инициалами
- [x] Dropdown меню (Профиль, Настройки, Выход)

### Dashboard (главная)
- [x] Компактная статистика (баннеры без скрываемого блока)
- [x] Фильтры в скрываемой карточке (по умолчанию свёрнуты)
- [x] MultiSelect компоненты для фильтров
- [x] DatePicker (flatpickr) со стилизацией под дизайн
- [x] Таблица объявлений (компактный режим, ~10 объявлений на экране)
- [x] 25 моковых записей с реальными московскими адресами
- [x] Колонка "Метро" с цветами линий московского метро
- [x] Телефон кликабельный (tel:) и жирный
- [x] Площадь жирным шрифтом
- [x] Адрес с переносом на 2 строки (фиксированная ширина)
- [x] Иконки действий горизонтально (появляются при наведении)
- [x] Пагинация + кнопки "Столбцы" и "Экспорт" внизу
- [x] Убран заголовок "Объявления" для экономии места

### Стилизация
- [x] Палитра AssetFinder (оливково-зелёный + золотой акцент)
- [x] Шрифт Plus Jakarta Sans
- [x] Material Icons
- [x] Адаптивная вёрстка

### Инфраструктура
- [x] React 18 + TypeScript + Vite
- [x] Zustand (state management)
- [x] Axios с interceptors
- [x] Nginx конфигурация для https://local.firstcall.com
- [x] Docker интеграция
- [x] Миграции БД (безопасные, не перезаписывают данные)

---

## 🔄 В процессе / Следующие шаги

### Приоритет 1: Подключение реальных данных
- [ ] API для получения списка объявлений
- [ ] Реальная статистика из API
- [ ] Работающие фильтры
- [ ] Пагинация с API

### Приоритет 2: Функционал таблицы
- [ ] Сортировка по колонкам
- [ ] Клик по объявлению → детальная информация
- [ ] Кнопка звонка → интеграция с телефонией
- [ ] Избранное

### Приоритет 3: Другие страницы
- [ ] Страница профиля
- [ ] Страница настроек
- [ ] Страница тарифов/биллинга

---

## 📁 Структура проекта

### Backend (PHP)

```
/home/sokol/first-call/
├── bootstrap/          # Инициализация приложения
│   ├── app.php        # Загрузка конфигурации и Eloquent
│   ├── container.php  # DI контейнер
│   ├── middleware.php # Глобальные middleware
│   └── routes.php     # Загрузка маршрутов
├── config/            # Конфигурационные файлы
├── db/migrations/     # Миграции базы данных
├── docs/api/          # OpenAPI документация
├── public/            # Точка входа (index.php)
├── routes/            # Определение маршрутов API
├── src/               # Исходный код
│   ├── Commands/      # Консольные команды
│   ├── Controllers/   # Контроллеры
│   ├── Middleware/    # PSR-15 middleware
│   ├── Models/        # Eloquent модели
│   ├── Services/      # Сервисный слой
│   └── Utils/         # Утилиты
└── docker/            # Docker конфигурации
```

### Frontend (React)

```
frontend-react/
├── src/
│   ├── components/
│   │   ├── Layout/          # Header, Sidebar, Layout
│   │   ├── UI/              # StatsCard, Badge, MultiSelect, DatePicker
│   │   └── Auth/            # TelegramLoginButton, ProtectedRoute
│   ├── pages/
│   │   ├── Dashboard/       # Главная страница (таблица объявлений)
│   │   └── Login/           # Страница авторизации
│   ├── stores/
│   │   └── authStore.ts     # Zustand store для авторизации
│   ├── services/
│   │   └── api.ts           # Axios клиент и API методы
│   ├── types/               # TypeScript типы
│   └── styles/
│       └── index.css        # Глобальные стили + CSS переменные
├── index.html
├── package.json
├── tsconfig.json
└── vite.config.ts
```

---

## 🔧 Технологический стек

### Backend
- **PHP** 8.3+
- **Framework:** Slim 4 + PHP-DI
- **ORM:** Eloquent (Laravel)
- **БД:** PostgreSQL 15 + PostGIS
- **Auth:** JWT (firebase/php-jwt)
- **HTTP:** PSR-7/PSR-15

### Frontend
- **React** 18 + TypeScript
- **Build:** Vite 5
- **State:** Zustand
- **HTTP:** Axios
- **Стили:** CSS (без фреймворков)
- **Шрифт:** Plus Jakarta Sans
- **Иконки:** Material Icons

### Инфраструктура
- **Docker** + Docker Compose
- **Nginx** (reverse proxy + SSL)
- **WSL2** (Windows Subsystem for Linux)

---

## 📚 Документация

Вся документация находится в папке `docs/`. См. [README.md](./README.md)

| Документ | Описание |
|----------|----------|
| [DEPLOYMENT.md](./DEPLOYMENT.md) | Запуск на локалке и production |
| [ARCHITECTURE.md](./ARCHITECTURE.md) | Архитектура приложения |
| [DATABASE.md](./DATABASE.md) | Схема БД (22 таблицы) |
| [API-QUICK-REFERENCE.md](./API-QUICK-REFERENCE.md) | Справка по API |
| [FRONTEND_REACT.md](./FRONTEND_REACT.md) | React фронтенд |
| [TELEGRAM_SETUP.md](./TELEGRAM_SETUP.md) | Настройка Telegram |
| [scripts/README.md](./scripts/README.md) | Консольные команды |

**Cursor IDE:** `.cursor/rules/firstcall-backend.mdc` — правила для AI