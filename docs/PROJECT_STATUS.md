# First Call CRM - Статус проекта

**Дата обновления:** 13 февраля 2026

---

## 📝 Контекст для AI (обновляй при смене задачи!)

### Последняя сессия
**Дата:** 13 февраля 2026
**Что делали:** CRM v2 — переход на объектно-ориентированную модель + UI-фиксы + документация

### ✅ Выполнено (13.02.2026): CRM v2 — Объектная модель

#### Новая бизнес-модель
CRM переведена с модели "Клиент → Стадии" на "Объект → Контакт → Связка":
- **Property (Объект)** — главная сущность: недвижимость с ценой, площадью, собственником
- **Contact (Контакт)** — покупатель/арендатор (простая карточка: имя, телефон, email)
- **ObjectClient (Связка)** — пара объект+контакт, именно здесь живёт воронка продаж
- Один объект → много контактов, один контакт → много объектов (M:N через object_clients)
- Объект может быть из базы объявлений (listing_id) или введён вручную

#### Backend
- **4 миграции:** `properties`, `contacts`, `object_clients`, `migrate_clients_to_new_model`
- **3 Eloquent модели:** `Property`, `Contact`, `ObjectClient`
- **3 сервиса:** `PropertyService`, `ContactService`, `ObjectClientService`
- **2 контроллера:** `PropertyController` (12 endpoint'ов), `ContactController` (6 endpoint'ов)
- **Маршруты:** `/properties/*` и `/contacts/*` с `SubscriptionMiddleware` + `AuthMiddleware`
- Старые `/clients/*` сохранены как DEPRECATED

#### Frontend
- **Типы:** `types/property.ts` — Property, Contact, ObjectClientItem, PipelineCard, DealType
- **API:** `propertiesApi` + `contactsApi` в `api.ts`
- **Store:** `propertyStore.ts` (Zustand) — viewMode, фильтры, deal_type, множественный выбор стадий
- **Страницы Properties/:**
  - `Properties.tsx` — список объектов (таблица + чипы-фильтры + пагинация)
  - `PropertyCard.tsx` — карточка объекта (характеристики + привязанные клиенты + собственник)
  - `PropertyForm.tsx` — форма объекта (2 режима: из базы / ручной ввод)
  - `Pipeline.tsx` — kanban (карточка = пара объект+контакт)
  - `ContactPicker.tsx` — модалка привязки контакта к объекту
  - `FunnelChart.tsx` — воронка продаж
  - `ListingPicker.tsx` — поиск по базе объявлений
  - `StageSettings.tsx` — настройка стадий воронки
- **Страницы Contacts/:**
  - `Contacts.tsx` — справочник контактов
  - `ContactCard.tsx` — карточка контакта (с привязанными объектами)
  - `ContactForm.tsx` — форма контакта
- **Навигация:** "Объекты" → `/properties`, "Контакты" → `/contacts`

#### UI-фиксы (12 багов)
- Иконки hover без подчёркивания, кнопки "Добавить клиента" в стиле, min-height для ContactPicker
- Ошибки API с понятными сообщениями, единый стиль "Не указан" (CSS specificity fix)
- Создание контакта из ContactPicker с авто-привязкой
- Listing field mapping: `room_id` → `room->code`, `square_meters` (не `area`)
- PostgreSQL integer overflow fix: поиск по `external_id` первым
- Кнопка "Добавить" в StageSettings: исправлен класс `btn btn-primary`

#### Тестирование (Фаза 8) — PASS
- **Миграции:** 58/58 выполнены успешно (`php db/migrations/run.php`)
- **TypeScript:** `npx tsc --noEmit` — 0 ошибок
- **Backend API:** 21/21 тест пройден (Property CRUD, Contact CRUD, ObjectClient attach/detach/moveToStage, Pipeline board, Stats, дубликаты, архив)
- **Production сборка:** `npm run build` — OK (927 KB JS, 197 KB CSS)
- **Frontend (Playwright):** 4 страницы проверены с реальными данными:
  - `/properties` — список объектов (таблица, фильтры, пагинация)
  - `/properties` kanban — колонки стадий с карточками
  - `/contacts` — справочник контактов
  - `/properties/{id}` — карточка объекта с привязанными клиентами

#### ReDoc API-документация — исправлена
- Загрузка `redoc.standalone.js` локально (без CDN) — устранена зависимость от jsdelivr
- Убрана зависимость от Google Fonts
- Исправлен `bearerAuth` → `BearerAuth` (case sensitivity) в deprecated `/clients` маршрутах
- Все 77 paths (18 новых CRM v2 операций) отображаются корректно

#### Документация обновлена
- `docs/DATABASE.md` — 35 таблиц, 58 миграций
- `docs/API-QUICK-REFERENCE.md` — секции CRM Объекты + Контакты + Связки
- `docs/api/openapi.yaml` — 18 новых операций, примеры ответов
- `docs/api/redoc.html` — локальный JS, без CDN
- `CLAUDE.md` — 35 таблиц, новые API endpoints, структура frontend
- `docs/PROJECT_STATUS.md` — актуализирован

### ✅ Выполнено (12.02.2026, сессия 2): Багфикс CRM v1

#### Реальные баги (4 шт)
- **Null dereference** в `updateCriteria`/`deleteCriteria` — добавлена проверка `!$criteria->client`
- **Утечка сообщений исключений** в подборках — `InvalidArgumentException` → 400, `Exception` → 500 с generic сообщением
- **reorder() без транзакции** — обёрнуто в `DB::connection()->transaction()`
- **Удаление стадии с архивными клиентами** — считаются ВСЕ клиенты, понятное сообщение об ошибке

#### N+1 производительность (4 шт)
- `getPipelineBoard()`: 10 запросов → 2 (один запрос клиентов + groupBy)
- `getPipelineSummary()`: 10 → 1-2 (withCount)
- `PipelineStageController::index()`: 10 → 1-2 (withCount)
- `getStats()`: 8 COUNT → 1 запрос с PostgreSQL `FILTER(WHERE)`

#### Валидация (3 шт)
- `reorder()` проверяет полноту массива стадий (count order == count stages)
- ILIKE-спецсимволы (`%`, `_`, `\`) экранируются в поиске клиентов
- `createDefaultStages()` обёрнут в транзакцию

#### Дополнительное укрепление безопасности
- Убрана утечка `exception->getMessage()` из всех catch-блоков (generic сообщения)
- Null-safe `getParsedBody()` во всех методах контроллера
- `InvalidArgumentException` для бизнес-ошибок в create/update/moveStage
- Валидация FK (category_id, location_id) перед INSERT в criteria
- `safeParseDate()` вместо голого `Carbon::parse()`

### ✅ Выполнено (12.02.2026): CRM модуль v1 (устаревшая модель)

#### Backend
- **4 миграции:** `pipeline_stages`, `clients`, `client_search_criteria`, `client_listings`
- **4 Eloquent модели:** `PipelineStage`, `Client`, `ClientSearchCriteria`, `ClientListing`
- **ClientService** — бизнес-логика CRM: CRUD клиентов, воронка, подборки, статистика
- **ClientController** — 15 endpoint'ов: список, карточка, CRUD, архив, стадии, подборки, критерии
- **PipelineStageController** — CRUD стадий воронки + reorder
- **Маршруты** — группа `/clients` с `SubscriptionMiddleware` + `AuthMiddleware` (DEPRECATED)
- **DI-контейнер** — зарегистрированы `ClientService`, `ClientController`, `PipelineStageController`

#### Также в этой сессии
- Исправлен баг `now()` → `Carbon::now()` в `AdminUserController.php:197`
- Закоммичена имперсонация (вход/выход под пользователем)
- Закоммичена миграция на Claude Code

### 🎯 Следующие шаги (CRM v2 — Фаза 2)
- [ ] Таймлайн/хронология взаимодействий (`client_interactions`)
- [ ] Напоминания с Telegram-уведомлениями (`client_reminders`)
- [ ] Авто-мэтчинг объявлений с критериями клиентов
- [ ] Кнопка "Предложить клиенту" в Dashboard
- [ ] Массовые операции с объектами

### ✅ Выполнено (11.02.2026): Миграция на Claude Code

#### 1. Конфигурация Claude Code
- **`CLAUDE.md`** — основные правила проекта (стек, архитектура, критические правила, навигация по docs/)
- **`.claudeignore`** — перенесено из `.cursorignore` + дополнения
- **`~/.claude/CLAUDE.md`** — глобальные правила разработки (стиль кода, PHP, безопасность, git)

#### 2. Slash-команды (`.claude/commands/`)
- `/dev` — запуск проекта (`make dev`)
- `/dev-stop` — остановка (`make dev-stop`)
- `/git-push` — полный цикл: fetch, merge, проверка правил, коммит, push
- `/status` — вывод статуса проекта из PROJECT_STATUS.md

#### 3. Документация
- **`docs/DATABASE.md`** — полностью переписан по актуальной схеме БД (30 таблиц, 54 миграции, 46 FK, 80 индексов)
- **`docs/SCRIPTS.md`** — перенесён из docs/scripts/README.md (5 консольных команд)
- **`docs/SUBSCRIPTIONS.md`** — переименован из subscriptions.md (UPPERCASE)

#### 4. Удалено
- Папка `.cursor/` (rules, commands, prompts, README) — заменена на Claude Code аналоги

#### 5. Актуализация данных БД
- Обнаружены расхождения документации с реальной схемой: 4 отсутствовавших таблицы, некорректные колонки в listings, устаревшие тарифы
- Всё исправлено в DATABASE.md и CLAUDE.md

---

### ❌ Удалено (12.02.2026): Агентства

Незакоммиченная работа по агентствам (08-09.02.2026) была полностью удалена:
- Удалены файлы: AgencyController, Agency, AgencyMember, AgencyService, миграции, frontend-страницы
- Удалена ложная документация agencies/agency_members из DATABASE.md
- Удалены офисные тарифы из описания

---

### ✅ Выполнено (08.02.2026): Авторизация источников (CIAN, Avito)

#### 1. Go-сервис cian-proxy
- **Расположение:** `services/cian-proxy/`
- **Docker контейнер:** `slim_cian-proxy` (порт 4829)
- **Функционал:** Прокси для запросов к CIAN/Avito с обходом защиты (CycleTLS)
- **Автозапуск:** `restart: unless-stopped`, healthcheck каждые 30 сек

#### 2. База данных
- **`user_source_cookies`** — хранение кук авторизации пользователей
- **Миграции:** `20260208000002_create_user_source_cookies_table.php`

#### 3. Backend API (`/api/v1/source-auth/`)
- `GET /status` — статус авторизации на источниках (CIAN и Avito)
- `POST /cookies` — сохранение кук (ручной ввод)
- `POST /revalidate` — перепроверка авторизации (обновление кук)
- `DELETE /cookies` — удаление авторизации

#### 4. Валидация кук
- **CIAN:** Проверка через `api.cian.ru/early-access/v1/check-contacts-access/` (через Go-прокси с proxy)
- **Avito:** Проверка через `www.avito.ru/web/2/profileinfo` (через Go-прокси без proxy)
- **Статусы CIAN:** `confirm`, `free`, `needPay` — все считаются валидными
- **При ошибке:** Старые куки НЕ перезаписываются невалидными новыми

#### 5. Frontend — вкладка "Авторизация источников"
- **Путь:** `/settings` → таб "Авторизация источников"
- **Компоненты:** `SourceAuth.tsx`, `SourceAuth.css`
- **Функционал:**
  - Инструкция по копированию кук из DevTools (Network → Headers → Cookie)
  - Ручной ввод кук в textarea
  - Отображение информации о подписке (CIAN: тариф, лимиты, телефон; Avito: имя, баланс, остаток размещений)
  - Кнопка "Перепроверить" — валидация текущих кук
  - Кнопка "Удалить авторизацию"

#### 6. Документация обновлена
- `docs/API-QUICK-REFERENCE.md` — все эндпоинты source-auth
- `docs/DATABASE.md` — таблица user_source_cookies
- `docs/api/openapi.yaml` — полное описание API с примерами

---

### ✅ Выполнено (08.02.2026): Уведомления и UI

#### 1. Защита от дубликатов уведомлений
- **Миграция:** `20260208000001_add_notification_tracking_to_subscriptions.php`
- **Новые поля в `user_subscriptions`:**
  - `notified_expiring_3d_at` — уведомление за 3 дня (премиум)
  - `notified_expiring_1d_at` — уведомление за 1 день (премиум)
  - `notified_expiring_1h_at` — уведомление за 1 час (демо)
  - `notified_expiring_15m_at` — уведомление за 15 минут (демо)
  - `notified_expired_at` — уведомление об истечении
- **Логика:** Атомарная блокировка через `UPDATE ... WHERE notified_*_at IS NULL` перед отправкой
- **Сброс при продлении:** Все поля сбрасываются в NULL в методе `extendByAdmin()`
- **Cron:** Скрипты запускаются каждые 10-30 минут без риска дубликатов

#### 1.1. Исправление скрипта уведомлений об истечении
- **Проблема:** `notify-subscription-expired` искал `status='active'`, но к моменту отправки статус уже `expired`
- **Решение:** Изменён поиск на `status='expired'` — скрипт `update-expired-subscriptions` сначала меняет статус, потом `notify-subscription-expired` отправляет уведомление

#### 2. Исправление UI скачков при загрузке
- **Кнопки Header:** Фиксированная высота `38px`, ширина `110px`
- **Material Icons:** Preload шрифта в `index.html`, глобальные стили с фиксированными размерами `24x24px`
- **Sidebar:** Убрана анимация появления секций меню

#### 3. Отключение кеширования на всех страницах
- Все страницы используют `staleTime: 0, gcTime: 0`
- Кнопка "Применить" всегда вызывает `refetch()`

#### 4. Демо-подписки без действий
- На страницах "Мои подписки" и "Admin Billing" для демо-подписок действия недоступны

### ✅ Выполнено (07.02.2026): Биллинг

#### 1. Страница "Мои подписки" (пользователь)
- **Путь:** `/billing`
- **Компонент:** `frontend-react/src/pages/Billing/Billing.tsx`
- **Функционал:**
  - Таблица подписок пользователя с колонками: ID, Дата создания, Тариф, Статус, Осталось, Начало, Окончание
  - Фильтры: ID подписки, дата создания (от/до), статус (мультивыбор)
  - Статусы с цветными бейджами: Активна (зелёный), Ожидает оплаты (оранжевый), Ожидает продления (синий), Истекла (красный), Отменена (серый)
  - Футер таблицы с информацией о количестве записей и выбором "на странице"
  - Пагинация (при нескольких страницах)
  - Кеширование отключено (staleTime: 0, gcTime: 0, refetchOnMount: 'always')

#### 2. Страница "Управление подписками" (админ)
- **Путь:** `/admin/billing`
- **Компонент:** `frontend-react/src/pages/AdminBilling/AdminBilling.tsx`
- **Функционал:**
  - Две вкладки: "Текущие подписки" и "История изменений"
  - **Текущие подписки:**
    - Таблица: ID, User ID, Telegram, Тариф, Статус, Осталось, Оплачено, Создана, Окончание, Заметки
    - Фильтры: ID подписки, ID пользователя, дата создания, осталось дней (мин/макс), статус
  - **История изменений:**
    - Таблица: ID, Подписка, User ID, Тариф, Было, Стало, Сумма, Дата, Заметки
    - Фильтры: ID подписки, ID пользователя, дата действия, тип действия
  - Пункт меню виден только админам (role === 'admin')

#### 3. API интеграция
- **billingApi** (`api.ts`):
  - `getUserSubscriptions(filters)` — POST /api/v1/billing/user-subscriptions
- **adminBillingApi** (`api.ts`):
  - `getCurrentSubscriptions(filters)` — POST /api/v1/billing/admin/current-subscriptions
  - `getSubscriptionHistory(filters)` — POST /api/v1/billing/admin/subscription-history

#### 4. Навигация
- Кнопка "Биллинг" в хедере → `/billing`
- Пункт "Мои подписки" в сайдбаре → `/billing`
- Пункт "Управление подписками" в сайдбаре (только админ) → `/admin/billing`

#### 5. Стили
- Контрастные таблицы с чередующимися строками
- Яркие цветные бейджи статусов
- Кнопки фильтров с hover-эффектами
- Адаптивная вёрстка

### Также выполнено (07.02.2026):

#### Supervisor для photo-tasks
- Создан конфиг `/home/sokol/first-call/docker/dev/php-cli/supervisor.d/photo-tasks.ini`
- Автозапуск и автоперезапуск при ошибках
- Логирование в `/var/log/supervisor/photo-tasks.log`

#### Исправления
- Исправлено скачивание фото (без перезагрузки страницы)
- AuthMiddleware поддерживает токен в query параметре для скачивания файлов
- PhotoTaskController получает ID через `$request->getAttribute('id')`

### Изменённые файлы (08.02.2026):
- `frontend-react/src/pages/AdminBilling/AdminBilling.tsx` — кнопки действий, модальные окна
- `frontend-react/src/pages/AdminBilling/AdminBilling.css` — стили для кнопок и модалок
- `frontend-react/src/services/api.ts` — методы activate, extend, cancel

### Изменённые файлы (07.02.2026):
- `frontend-react/src/pages/Billing/` — новая страница
- `frontend-react/src/pages/AdminBilling/` — новая страница
- `frontend-react/src/services/api.ts` — billingApi, adminBillingApi
- `frontend-react/src/App.tsx` — роуты /billing, /admin/billing
- `frontend-react/src/components/Layout/Sidebar.tsx` — пункты меню
- `frontend-react/src/components/Layout/Header.tsx` — кнопка Биллинг
- `frontend-react/src/components/Layout/Sidebar.css` — стили админского пункта
- `docker/dev/php-cli/supervisor.d/photo-tasks.ini` — конфиг Supervisor
- `src/Middleware/AuthMiddleware.php` — поддержка токена в query
- `src/Controllers/PhotoTaskController.php` — исправлено получение ID

---

### ✅ Выполнено (08.02.2026): Действия админа

#### Страница "Управление подписками" — действия админа
- **Кнопки действий** в таблице подписок:
  - ✅ Активировать (для pending и expired) — зелёная иконка
  - 🔄 Продлить (для active) — синяя иконка
  - ❌ Отменить (для pending и active) — красная иконка
- **Модальные окна** для каждого действия:
  - Информация о подписке (ID, пользователь, тариф, статус)
  - Форма с полями: метод оплаты, сумма, продолжительность, примечания
  - Предупреждение при отмене
  - Обработка ошибок
- **API интеграция** (`adminBillingApi`):
  - `activateSubscription(subscriptionId, paymentMethod, notes?, durationHours?)`
  - `extendSubscription(subscriptionId, paymentMethod, price?, notes?, durationHours?)`
  - `cancelSubscription(subscriptionId, reason?)`
- **Автообновление** таблицы после действий (invalidateQueries)

---

### Текущий фокус
- CRM v2 (объектная модель) — **полностью завершена**: backend, frontend, тестирование, документация, ReDoc
- **Следующая задача:** CRM v2 — Фаза 2:
  - Таймлайн/хронология взаимодействий (`client_interactions`)
  - Напоминания с Telegram-уведомлениями (`client_reminders`)
  - Авто-мэтчинг объявлений с критериями клиентов
  - Кнопка "Предложить клиенту" в Dashboard
  - Массовые операции с объектами

### Важно помнить
- **Парсинг ответа API:** `response.data` = `{ meta, data }` (без обёртки success)
- **Кеширование отключено** для ВСЕХ страниц (staleTime: 0, gcTime: 0, refetch() при применении фильтров)
- **Админский доступ:** проверяется `user?.role === 'admin'`
- **PHP-DI + Slim 4:** Параметры роута через `$request->getAttribute('id')`
- **Скачивание файлов:** токен передаётся в query параметре `?token=...`
- **Миграции:** Все изменения БД ТОЛЬКО через миграции, не напрямую SQL
- **Уведомления:** Защита от дубликатов через поля `notified_*_at` в `user_subscriptions`
- **CRITICAL:** Никогда не использовать `now()` — только `Carbon::now()` с `use Carbon\Carbon;`
- **Конфигурация IDE:** Проект мигрирован на Claude Code. Правила в CLAUDE.md, команды в .claude/commands/

### Блокеры
- Нет активных блокеров

### Следующие шаги
1. CRM v2 Фаза 2: таймлайн, напоминания, авто-мэтчинг
2. Добавить очистку старых архивов (cron)
3. Реализовать WebSocket для real-time обновлений
4. Интеграция с телефонией (автозвонок)

---

## 🚀 Быстрый старт

### Шаг 1: Запуск Backend + БД (Docker)

```bash
cd /home/sokol/first-call

# Запуск всех контейнеров
docker-compose up -d

# Или через Makefile
make up

# Проверить статус
docker-compose ps
```

**Что запускается:**
| Контейнер | Сервис | Порт |
|-----------|--------|------|
| slim_postgres | PostgreSQL 15 + PostGIS | 5432 |
| slim_php-fpm | PHP-FPM 8.3 | 9000 (internal) |
| slim_php-cli | PHP CLI (команды) | — |
| slim_nginx | Nginx (reverse proxy + SSL) | 80, 443 |
| slim_pgadmin | pgAdmin 4 | 5050 |

### Шаг 2: Запуск Frontend (React)

```bash
cd /home/sokol/first-call/frontend-react

# Установить зависимости (если не установлены)
npm install

# Запуск dev-сервера
npm run dev
# По умолчанию: http://localhost:3000

# Или с указанием порта
npm run dev -- --port 3002 --host
```

### Шаг 3: Запуск скрипта обработки фото

```bash
# Ручной запуск (для тестирования)
docker exec -it slim_php-cli php bin/app.php photo-tasks

# Через Supervisor (автоматически при запуске контейнера)
# Конфиг: docker/dev/php-cli/supervisor.d/photo-tasks.ini
```

### Шаг 4: Сборка для production

```bash
cd /home/sokol/first-call/frontend-react

# Сборка
npm run build

# Результат в папке dist/, раздаётся через Nginx
```

---

## 🌐 URLs

| Сервис | URL | Описание |
|--------|-----|----------|
| **Приложение (prod)** | https://local.firstcall.com | Через Nginx + SSL |
| **Frontend dev** | http://localhost:3000 | Vite dev server |
| **API** | https://local.firstcall.com/api/v1 | REST API |
| **pgAdmin** | http://localhost:5050 | Управление БД |
| **API документация** | http://localhost:8090/redoc.html | OpenAPI/Redoc |

---

## 🗄️ База данных

### Подключение к pgAdmin (http://localhost:5050)

**Логин в pgAdmin:**
- Email: `admin@example.com`
- Password: `admin`

**Добавление сервера PostgreSQL:**
- Host: `postgres` (имя контейнера в Docker network)
- Port: `5432`
- Database: `slim_api`
- Username: `postgres`
- Password: `postgres`

### Миграции

```bash
# Запуск миграций
make migrate

# Или вручную
docker exec -it slim_php-cli php db/migrations/run.php
```

---

## 🛠️ Полезные команды

### Docker

```bash
# Статус контейнеров
docker-compose ps

# Логи всех сервисов
docker-compose logs -f

# Войти в контейнер PHP
docker exec -it slim_php-cli bash
```

### Консольные команды

```bash
# Перенос объявлений
docker exec -it slim_php-cli php bin/app.php transfer:listings

# Обработка фото (daemon)
docker exec -it slim_php-cli php bin/app.php photo-tasks

# Парсинг метро
docker exec -it slim_php-cli php bin/app.php metro:parse
```

### Frontend

```bash
cd frontend-react

# Dev сервер
npm run dev

# Сборка
npm run build

# Проверка типов TypeScript
npx tsc --noEmit
```

---

## 📊 Статус коммитов (на 13.02.2026)

### Последние коммиты
| Коммит | Описание |
|--------|----------|
| (новый) | feat: CRM v2 — объектная модель, тестирование, ReDoc fix |
| `9f11453` | docs: Финальное обновление PROJECT_STATUS.md за 12.02.2026 |
| `d38f3de` | feat: Кастомные агенты — bug-fixer и code-auditor |
| `dfbfd00` | fix: Дополнительное укрепление безопасности CRM контроллеров |
| `ba2c601` | fix: Исправлены 11 багов и проблем производительности CRM модуля |
| `de2648d` | feat: CRM модуль — клиенты, воронка продаж, подборки объявлений |

### Незакоммиченные изменения
Нет

---

## ✅ Что сделано

### Миграция на Claude Code (11.02.2026)
- [x] CLAUDE.md — правила проекта для автозагрузки
- [x] .claudeignore — исключения файлов
- [x] Slash-команды: /dev, /dev-stop, /git-push, /status
- [x] DATABASE.md переписан по реальной БД
- [x] SCRIPTS.md и SUBSCRIPTIONS.md оформлены

### Действия админа (08.02.2026)
- [x] Кнопки действий в таблице подписок (активация, продление, отмена)
- [x] Модальные окна с формами для каждого действия
- [x] API интеграция с бэкендом
- [x] Автообновление таблицы после действий

### Биллинг (07.02.2026)
- [x] Страница "Мои подписки" для пользователей
- [x] Страница "Управление подписками" для админов
- [x] Фильтры, пагинация, статусы с цветными бейджами
- [x] Supervisor для автозапуска photo-tasks
- [x] Исправлено скачивание фото без перезагрузки страницы

### Обработка фото (06.02.2026)
- [x] Таблица photo_tasks (одна задача на объявление)
- [x] Сервис PhotoTaskService с интеграцией UNMARK API
- [x] Консольная команда photo-tasks (daemon mode)
- [x] API endpoints: создание задачи, скачивание архива
- [x] UI: кнопка с динамической иконкой и статусом
- [x] Повторная попытка при ошибке (сброс на pending)

### Улучшения Dashboard (02.02.2026)
- [x] Кнопка просмотра описания объявления
- [x] История изменения цен (price_history)
- [x] Статистика в баннерах с трендами
- [x] Красивые тултипы (Tooltip компонент)
- [x] Подсветка поднятых объявлений

### Избранное (29.01.2026)
- [x] Таблица избранных объявлений с пагинацией
- [x] Пользовательские статусы (CRUD)
- [x] Комментарии к избранным
- [x] Фильтры и сортировка

### Авторизация
- [x] Telegram Login Widget интеграция
- [x] JWT токены (access + refresh)
- [x] Автоматическое обновление токенов

### Dashboard (главная)
- [x] Таблица объявлений с фильтрами
- [x] Пагинация и сортировка
- [x] Polling каждые 3 секунды
- [x] Звуковые уведомления о новых объявлениях

---

## 📜 История изменений (changelog)

### 13 февраля 2026
- ✅ CRM v2: переход на объектную модель (Property → Contact → ObjectClient)
- ✅ 4 миграции: properties, contacts, object_clients, migrate_clients_to_new_model
- ✅ Backend: PropertyService, ContactService, ObjectClientService, PropertyController, ContactController
- ✅ Frontend: Properties/ (8 компонентов), Contacts/ (3 компонента), навигация, store
- ✅ 12 UI-багов: integer overflow, CSS specificity, listing field mapping, ContactPicker и др.
- ✅ Тестирование: 21/21 backend, TSC 0 ошибок, build OK, 4 страницы Playwright
- ✅ ReDoc: локальный JS (без CDN), исправлен bearerAuth → BearerAuth
- ✅ Документация: DATABASE.md, API-QUICK-REFERENCE.md, openapi.yaml, CLAUDE.md, PROJECT_STATUS.md

### 12 февраля 2026
- ✅ CRM модуль Фаза 1 (v1): клиенты, воронка продаж, подборки объявлений (backend + frontend + документация)
- ✅ Имперсонация: вход/выход под пользователем (AdminUserController, AuthController)
- ✅ Исправлен баг `now()` → `Carbon::now()` в AdminUserController
- ✅ Удалена ложная документация agencies/agency_members
- ✅ Багфикс CRM: 4 бага, 4 N+1, 3 проблемы валидации
- ✅ Укрепление безопасности: утечка сообщений, null-safe getParsedBody, safeParseDate, FK-валидация

### 11 февраля 2026
- ✅ Миграция с Cursor IDE на Claude Code CLI
- ✅ Создан CLAUDE.md (основные правила проекта, ~300 строк)
- ✅ Создан .claudeignore (перенос из .cursorignore)
- ✅ 4 slash-команды: /dev, /dev-stop, /git-push, /status
- ✅ Глобальные правила в ~/.claude/CLAUDE.md
- ✅ DATABASE.md полностью переписан по реальной схеме БД (30 таблиц вместо 26)
- ✅ SCRIPTS.md и SUBSCRIPTIONS.md — переименованы/перенесены
- ✅ Удалена конфигурация .cursor/ (заменена на .claude/)

### 8-9 февраля 2026 (НЕ ЗАКОММИЧЕНО)
- ⚠️ Начата работа над функционалом агентств (офисные тарифы)
- ⚠️ 4 миграции: office поля, agencies, agency_members, office тарифы
- ⚠️ Backend: AgencyController, Agency, AgencyMember, AgencyService
- ⚠️ Frontend: страница Agency/, ConfirmModal
- ⚠️ Impersonate — не доделан
- ⚠️ Модифицированы: routes, middleware, контроллеры, модели, api.ts, App.tsx

### 8 февраля 2026
- ✅ Реализована авторизация на источниках (CIAN, Avito)
- ✅ Go-сервис cian-proxy в Docker (порт 4829)
- ✅ Новые таблицы: user_source_cookies, bookmarklet_tokens
- ✅ API эндпоинты для управления авторизацией
- ✅ UI: новая вкладка в настройках с Bookmarklet и ручным вводом
- ✅ Добавлены кнопки действий админа в таблицу подписок
- ✅ Реализованы модальные окна для активации, продления и отмены подписок
- ✅ Интеграция с существующим API бэкенда
- ✅ Автообновление данных после выполнения действий
- ✅ Защита от дубликатов уведомлений (поля notified_*_at + атомарная блокировка)
- ✅ Исправлены скачки UI при загрузке страницы (Material Icons preload, фиксированные размеры)
- ✅ Отключено кеширование на всех страницах (refetch при применении фильтров)
- ✅ Демо-подписки без действий на страницах биллинга

### 7 февраля 2026
- ✅ Реализована страница "Мои подписки" для пользователей
- ✅ Реализована страница "Управление подписками" для админов
- ✅ Настроен Supervisor для автозапуска photo-tasks
- ✅ Исправлено скачивание фото (без перезагрузки страницы)
- ✅ Улучшены стили таблиц (контрастность, читаемость)

### 6 февраля 2026
- ✅ Реализован функционал обработки фото (удаление водяных знаков)
- ✅ Интеграция с UNMARK API
- ✅ Консольная команда photo-tasks (daemon mode)
- ✅ UI кнопка с динамическим статусом
- ✅ Протестировано: 2 задачи, 50 фото обработано

### 2 февраля 2026
- ✅ Улучшения Dashboard: описание, история цен, статистика с трендами
- ✅ Компонент Tooltip для красивых подсказок
- ✅ Кнопка очистки комментария в Избранном

### 1 февраля 2026
- ✅ Исправлена ошибка 401 при редактировании статусов (PHP-DI + Slim 4)
- ✅ Исправлен CORS middleware

### 29 января 2026
- ✅ Реализована страница "Избранное" с полным функционалом

---

## 🎯 Roadmap

### Q1 2026
- [x] Обработка фото (удаление водяных знаков)
- [x] Настройка Supervisor для daemon-скриптов
- [x] Страницы биллинга (пользователь + админ)
- [x] Действия админа (активация/продление/отмена подписки)
- [x] CRM v1: клиенты, воронка, подборки
- [x] CRM v2: объектная модель (Property → Contact → ObjectClient)
- [ ] CRM v2 Фаза 2: таймлайн, напоминания, авто-мэтчинг
- [ ] WebSocket для real-time обновлений
- [ ] Интеграция с телефонией (автозвонок)

### Q2 2026
- [ ] Мобильное приложение
- [ ] Расширение на другие города
- [ ] Интеграция с CRM системами
